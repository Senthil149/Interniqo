{
  "name": "internship-platform-blockchain",
  "version": "0.0.1",
  "private": true,
  "description": "Hardhat project for internship credential contracts",
  "scripts": {
    "node": "hardhat node",
    "compile": "hardhat compile",
    "health": "node scripts/health-check.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.28.4"
  }
}
